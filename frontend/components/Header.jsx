import AddQuoteForm from "./AddQuoteForm";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <header>
      <NavBar />
      <AddQuoteForm />
    </header>
  );
};

export default Header;
